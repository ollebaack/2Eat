// This component has been merged into RecipeDetailPage.tsx (2EA-68).
// The file is kept as a placeholder to avoid broken imports if referenced elsewhere.
// The responsive layout is now handled entirely within RecipeDetailPage using
// Tailwind CSS classes (block md:hidden / hidden md:block).
